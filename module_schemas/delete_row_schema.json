{
  "type": "object",
  "required": ["sheet_id", "sheet_title", "row_index"],
  "properties": {
    "sheet_id": {"type": "string"},
    "sheet_title": {"type": "string", "description": "Tab name, e.g., 'Sheet1'"},
    "row_index": {"type": "integer", "minimum": 0, "description": "0-based index"}
  }
}
